exports.run = (client) => {
  const config = require('./config.json');
  client.user.setPresence({ game: { name: 'Rock Store', type: 1, url: 'https://www.twitch.tv/lorritatv'} });
  console.log(`Estou agora neste momento no servidor da Rock Store!`);
}
